# List all Docker images available locally
docker image ls

# Pull the latest version of the Redis image from Docker Hub
docker image pull redis:latest

# Remove the latest version of the Redis image from local storage
docker image rm redis:latest
